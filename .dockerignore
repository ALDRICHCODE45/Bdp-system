# Dependencies — Docker instala las suyas en la imagen
node_modules
.pnp
.pnp.*

# Build output — se regenera dentro del builder stage
.next
out
build

# Dev & local config — NUNCA deben entrar a la imagen
.env
.env.*
!.env.example

# Git
.git
.gitignore

# Editor / OS
.vscode
.cursor
.DS_Store
*.pem

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
build-output.log

# TypeScript incremental build info
*.tsbuildinfo
next-env.d.ts

# Prisma generated client (se regenera en entrypoint)
src/generated/prisma

# Docker itself
Dockerfile
.dockerignore
docker-compose.yml

# CI/CD
.github
